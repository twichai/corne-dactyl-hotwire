if DCORNE_LEFT

# Name must be less than 16 characters long!
config ZMK_KEYBOARD_NAME
    default "dcorne"

config ZMK_SPLIT_ROLE_CENTRAL
    default y

endif

if DCORNE_LEFT || DCORNE_RIGHT

config ZMK_SPLIT
    default y

endif