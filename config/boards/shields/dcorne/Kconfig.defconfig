if CORNE_LEFT

# Name must be less than 16 characters long!
config ZMK_KEYBOARD_NAME
    default "corne"

config ZMK_SPLIT_ROLE_CENTRAL
    default y

endif

if CORNE_LEFT || CORNE_RIGHT

config ZMK_SPLIT
    default y

endif